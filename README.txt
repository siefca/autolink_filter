This module adds a filter to automatically link known taxonomy terms.


TODO:

add setting 'minimum word length' (0 - 32) i 'maximum word length' (0 - 1024 - any)
defaults: 2, 32

add setting 'excluded words'
defaults: or and but no yes though because they i you he she it since are were have had been since m very less also to too

add setting 'leafs only'
defaults: true

add setting for content type selection

add setting: [ ] no tooltips  [ ] tooltips from descriptions  [ ] tooltips from field_tooltip

add setting: [ ] links + tooltips  [  ] links only  [  ] tooltips only

