This module adds a filter to automatically link known taxonomy terms.

TODO:

add setting 'minimum word length' (0 - 32) and 'maximum word length' (0 - 1024 - any)
defaults: 2, 32

- add setting for content type or content type / content field selection (e.g. 'articles only'),
